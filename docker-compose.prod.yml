version: '3'

services:
  backend:
    image: ${ARG_REPOS_URL}
    build:
      context: ./containers/backend
      dockerfile: ./build/Dockerfile
    tty: true
    restart: always
    ports:
      - 80:80
    volumes:
      - ./containers/backend:/go/src/api
